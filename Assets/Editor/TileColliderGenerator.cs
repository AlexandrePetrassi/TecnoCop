using UnityEngine;
using System.Collections;
using UnityEditor;
using UnityEditor.UI;

public class TileColliderGenerator : EditorWindow {
	[MenuItem ("Window/TileColliderGenerator")]
	static void Init(){
		//TileColliderGenerator window = (TileColliderGenerator)EditorWindow.GetWindow(typeof(TileColliderGenerator));
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
